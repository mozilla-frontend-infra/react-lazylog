// Should be removed in the future
import React from 'react';
import { render } from 'react-dom';
const root = document.getElementById('root');
import NameForm from './content';

render(<NameForm />, root);
